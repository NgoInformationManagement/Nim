{% extends 'NIMWebBundle::layout.html.twig' %}

{% import 'NIMWebBundle:Macro:button.html.twig' as buttons %}
{% from 'NIMWebBundle:Macro:misc.html.twig' import pagination %}
{% from 'NIMWebBundle:Agency:macro.html.twig' import list %}

{% block page_name %}
    <i class="icon-rocket"></i>
    {{ 'agency.title.index'|trans }}
{% endblock page_name %}


{% block page_action %}
    {{ buttons.new(path('nim_agency_create'), 'agency.button.new') }}
{% endblock page_action %}


{% block content %}
    {{ pagination(agencies) }}
    {{ list(agencies) }}
    {{ pagination(agencies) }}
{% endblock %}