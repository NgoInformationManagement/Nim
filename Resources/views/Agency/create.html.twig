{% extends 'NIMWebBundle::layout.html.twig' %}

{% import 'NIMWebBundle:Macro:button.html.twig' as buttons %}

{% block page_name %}
    <i class="icon-rocket"></i>
    {{ 'agency.title.create'|trans }}
{% endblock page_name %}


{% block page_action %}
    {{ buttons.back(path('nim_agency_index'), 'button.backToList') }}
    {{ buttons.create('button.create', 'agencyCreateForm') }}
{% endblock page_action %}


{% block content %}
    <form action="{{ path('nim_agency_create') }}"
          method="post"
          role="form"
          id="agencyCreateForm"
          class="form-horizontal"
          novalidate>
        {% include 'NIMWebBundle:Agency:_form.html.twig' %}
    </form>
{% endblock %}